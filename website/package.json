{
  "name": "docusaurus-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd website && docusaurus start",
    "build": "cd website && docusaurus build --out-dir ../build",
    "swizzle": "cd website && docusaurus swizzle",
    "deploy": "cd website && docusaurus deploy"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.55",
    "@docusaurus/preset-classic": "^2.0.0-alpha.55",
    "classnames": "^2.2.6",
    "react": "^16.8.4",
    "react-dom": "^16.8.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
