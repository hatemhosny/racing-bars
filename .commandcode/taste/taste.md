# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# svelte
- Use Svelte 5 runes syntax (e.g., `$state`, `$effect`, `$derived`) instead of Svelte 4 lifecycle methods (`onMount`, `onDestroy`). Confidence: 0.65

